/**
 * Agtk system layers constants interface module.
 *
 * @packageDocumentation
 */

/**
 * Agtk system layers constants interface.
 */
export interface AgtkSystemLayers {
  /**
   * System layer all ID.
   */
  readonly SystemLayerAllId: -1;

  /**
   * System layer background ID.
   */
  readonly SystemLayerBackgroundId: -2;

  /**
   * HUD layer ID.
   */
  readonly HudLayerId: 9999;
}
